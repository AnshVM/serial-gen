export type Model = {
  name: string
  code: string
  productName: string
}

export type SerialNumber = {
  serial: string
  company: string
  sequence: number
  createdAt: Date
  modelName: string
}
